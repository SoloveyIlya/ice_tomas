# Интеграция Ice Tomas с Laravel

Этот файл содержит инструкции по интеграции верстки Ice Tomas с Laravel.

## 📋 Шаги по установке

### 1️⃣ Создание нового проекта Laravel

Если у вас ещё нет установленного Laravel, выполните следующие шаги:

1. **Установите Composer** (если ещё не установлен):
   - Скачайте с официального сайта: https://getcomposer.org/
   - Следуйте инструкциям по установке

2. **Создайте новый проект Laravel**:
   ```bash
   composer create-project --prefer-dist laravel/laravel ice-tomas
   ```

3. **Перейдите в каталог проекта**:
   ```bash
   cd ice-tomas
   ```

### 2️⃣ Копирование файлов

Скопируйте файлы из папки `laravel-integration` в ваш Laravel проект:

1. **Скопируйте Blade-шаблоны**:
   ```bash
   cp -r laravel-integration/resources/views/* ice-tomas/resources/views/
   ```

2. **Скопируйте статические файлы** (CSS, JS, изображения):
   ```bash
   cp -r laravel-integration/public/css ice-tomas/public/
   cp -r laravel-integration/public/js ice-tomas/public/
   cp -r laravel-integration/public/media ice-tomas/public/
   cp laravel-integration/public/favicon.ico ice-tomas/public/
   ```

3. **Обновите маршруты**:
   ```bash
   cp laravel-integration/routes/web.php ice-tomas/routes/web.php
   ```

   Или откройте файл `ice-tomas/routes/web.php` и добавьте:
   ```php
   Route::get('/', function () {
       return view('home');
   });
   ```

### 3️⃣ Структура файлов

После копирования структура должна выглядеть так:

```
ice-tomas/
├── resources/
│   └── views/
│       ├── layouts/
│       │   └── app.blade.php
│       └── home.blade.php
├── public/
│   ├── css/
│   │   ├── main-page.css
│   │   ├── components/
│   │   └── helpers/
│   ├── js/
│   │   └── components/
│   │       ├── header.js
│   │       ├── slider.js
│   │       └── animations.js
│   ├── media/
│   │   ├── cards/
│   │   ├── svgicon/
│   │   └── ...
│   └── favicon.ico
└── routes/
    └── web.php
```

### 4️⃣ Запуск проекта

1. **Запустите встроенный сервер Laravel**:
   ```bash
   php artisan serve
   ```

2. **Откройте в браузере**:
   ```
   http://localhost:8000
   ```

Теперь должна отобразиться ваша верстка с учётом подключения всех стилей и скриптов.

## 📝 Важные замечания

1. **Пути к статическим файлам**: Все пути к изображениям, CSS и JS файлам используют Laravel helper `asset()`, который автоматически генерирует правильные URL.

2. **Blade-шаблоны**: 
   - `layouts/app.blade.php` - основной layout с `<head>` и подключением стилей/скриптов
   - `home.blade.php` - главная страница, которая расширяет layout

3. **Маршруты**: Маршрут для главной страницы уже настроен в `routes/web.php`.

## 🔧 Дальнейшая настройка

После успешной интеграции вы можете:

- Добавить контроллеры для обработки форм
- Настроить базу данных для хранения данных
- Добавить дополнительные страницы
- Настроить маршруты для других страниц (каталог, контакты и т.д.)

## ❓ Решение проблем

Если стили или скрипты не загружаются:

1. Проверьте, что файлы скопированы в правильные директории
2. Убедитесь, что пути в `layouts/app.blade.php` используют `asset()`
3. Очистите кэш Laravel: `php artisan cache:clear`
4. Проверьте права доступа к файлам в папке `public/`

